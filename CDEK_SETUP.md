# Интеграция СДЭК

В проект добавлен минимальный модуль интеграции со СДЭК API v2.

## Что реализовано

1. Получение OAuth-токена СДЭК.
2. Расчёт стоимости доставки через `/calculator/tariff`.
3. Создание заказа доставки через `/orders`.
4. Сохранение в заказе стоимости доставки, срока, тарифа, кода города, UUID СДЭК, ответа API и статуса интеграции.
5. Отображение СДЭК на странице оформления заказа.
6. Отображение данных СДЭК в Django Admin.

## Файлы, которые изменены

- `Project_Nankuev_A/settings.py`
- `requirements.txt`
- `main/models.py`
- `main/admin.py`
- `main/migrations/0002_order_cdek_fields.py`
- `main/services/cdek.py`
- `catalog/views.py`
- `templates/pages/checkout.html`
- `static/styles.css`

## Как запустить локально

1. Установить зависимости:

```bash
pip install -r requirements.txt
```

2. Применить миграции:

```bash
python manage.py migrate
```

3. Запустить сервер:

```bash
python manage.py runserver
```

По умолчанию включён демо-режим:

```python
CDEK_DEMO_MODE = True
```

В демо-режиме проект не отправляет запросы во внешний API, а рассчитывает учебную стоимость доставки локально. Это нужно, чтобы checkout работал без договора и ключей СДЭК.

## Как включить реальный API

В переменных окружения задать:

```bash
CDEK_DEMO_MODE=false
CDEK_BASE_URL=https://api.edu.cdek.ru/v2
CDEK_CLIENT_ID=ваш_client_id
CDEK_CLIENT_SECRET=ваш_client_secret
CDEK_FROM_CITY_CODE=44
```

Для боевого контура заменить `CDEK_BASE_URL` на боевой адрес API СДЭК и использовать боевые ключи.

## Что осталось как упрощение

- Код города СДЭК вводится вручную.
- Нет выбора ПВЗ на карте.
- Нет автоматического обновления трек-номера и статусов доставки.
- Габариты и вес посылки рассчитываются упрощённо: 300 г на единицу товара.

Эти пункты можно указать в отчёте как направления дальнейшего развития.
